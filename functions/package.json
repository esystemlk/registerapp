{
  "name": "smartlabs-functions",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "deploy": "firebase deploy --only functions",
    "lint": "echo \"no linter configured\""
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^6.2.0",
    "@sendgrid/mail": "^8.1.0",
    "googleapis": "^128.0.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
